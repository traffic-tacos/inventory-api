apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-api
  labels:
    app: inventory-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-api
  template:
    metadata:
      labels:
        app: inventory-api
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
      containers:
        - name: inventory-api
          image: ${{ vars.ECR_BASE_REPO }}/traffic-tacos-inventory-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8020
            - name: health
              containerPort: 8021
          readinessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 5
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          # resources:
          #   requests: { cpu: "100m", memory: "128Mi" }
          #   limits:   { cpu: "500m", memory: "512Mi" }
